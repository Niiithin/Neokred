export default {
  rootStyles: (theme: any) => ({
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    marginTop: theme.spacing(8),
  }),
  navigateText: (theme: any) => ({
    color: theme.palette.primary.main,
    marginBottom: theme.spacing(4),
  }),
};
